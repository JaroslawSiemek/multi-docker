name: Deploy MultiDocker
on:
  push:
    branches:
      - maaster # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t jerrysiemek/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true jerrysiemek/react-test npm test

      - run: docker build -t jerrysiemek/multi-client ./client
      - run: docker build -t jerrysiemek/multi-nginx ./nginx
      - run: docker build -t jerrysiemek/multi-server ./server
      - run: docker build -t jerrysiemek/multi-worker ./worker

      - run: docker push jerrysiemek/multi-client
      - run: docker push jerrysiemek/multi-nginx
      - run: docker push jerrysiemek/multi-server
      - run: docker push jerrysiemek/multi-worker
